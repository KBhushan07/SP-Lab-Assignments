START:
READ    N
LOAD    ZERO
STORE   COUNT
STORE   SUM
LOOP:
READ    X
LOAD    X
ADD     SUM
STORE   SUM
LOAD    COUNT
ADD     ONE
STORE   COUNT
SUB     N
JZ      OUTER:
JMP     LOOP:
OUTER:
WRITE   SUM
HLT
ENDP
ZERO    CONST   0
ONE     CONST   1
SUM     DB
COUNT   DB
N       DB
X       DB
END